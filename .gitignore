# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Audit artifacts (Puppeteer mobile + SEO audit)
audit-screenshots/
audit-results.json
build.log
tmp/

# Impeccable skill brand context (local only)
.impeccable.md

# Claude Code session scratchpad (local only)
.claude/scratchpad.md

# DaytonaCargo landmask source polygons (250KB, regenerable via generate-landmask.mjs)
scripts/world.geo.json
__pycache__/

# Local agent tooling (Codex CLI) — machine-local, never committed
.codex/
.agents/

# CAB-LOADS: runtime-smoke screenshots (local only)
tmp_cabloads_smoke/
