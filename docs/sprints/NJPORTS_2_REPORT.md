# NJPORTS-2 — Sprint Report

**Repo:** Y7-WEBSITE · **Branch:** `wip/njports-2` · **Date:** 2026-07-24 · **Status:** HELD — nothing pushed
**Depends on:** `[NJPORTS-T4]` (SEO snapshot baseline)

New evidence-backed page explaining how carrier pricing to the NJ export warehouses is formed, plus
additive crosslinks from three existing pages and a carried-over compliance fix. Every published
figure traces to the NJPORTS-2 fact table (§2 of the prompt).

---

## ⚠ Branch-base deviation (measured, not improvised)

The prompt says "Branch `wip/njports-2` from current `main`." On this machine that would have **failed
the sprint's own dependency**: measured this session,

- `main` = `6ddd26e` (only the `[NJPORTS-A0]` audit) — `scripts/seo-snapshot.mjs` is **absent** on main
  (`git cat-file -e main:scripts/seo-snapshot.mjs` → missing), so `npm run seo:snapshot` (the pre-check)
  cannot run there.
- `wip/njports-1` = `3aecc06` holds all of NJPORTS-1 (T1–T4, incl. the SEO baseline this sprint requires).

The NJPORTS-1 work is HELD on `wip/njports-1` and never reached `main`. The only ref satisfying the
`[NJPORTS-T4]` dependency is `wip/njports-1`, so **`wip/njports-2` was branched from `wip/njports-1`**.
The prompt's "from main" assumes prior sprints merged to main; they have not on this HELD-work machine.
Flagging for reconciliation.

## Pre-check

`git fetch --all --prune` clean · `git log --oneline main..origin/main` empty · working tree clean ·
`npm run seo:snapshot` wrote the baseline (10 routes) with no change vs the committed T4 baseline.

---

## T1 — New page

- **URL:** `/nj-export-warehouse-shipping-cost` (EN-only, `SeoLandingPage` template)
- **File:** `src/pages/seo/NjExportWarehouseShippingCost.jsx`
- **Registered in:** `src/App.jsx` (import + route), `scripts/generateSitemap.js` (`PAGE_SOURCE` +
  `ENGLISH_ONLY`), `scripts/prerender.mjs` (route list), `scripts/seo-snapshot.mjs` (`ROUTES`).
- **Word count:** ~1,500 words of body content (~2,140 including page chrome).
- **Schema inventory (validated, parses):** `BreadcrumbList` + `FAQPage` (6 questions) + `Service`
  (auto-generated by the template), plus the root `LocalBusiness`. `EntityTldr` "in brief" block present
  for AI extraction (pattern from `/auction-to-port-transport`).
- **Content spine (all 9 sections rendered, verified in the browser):** What Actually Sets the Price ·
  Why the Cost Per Mile Falls · Auction vs. Dealer/Private Pickup · Which Auction Doesn't Matter ·
  Vehicle Size · Where in NJ You Deliver Doesn't Matter · What Moves the Market · Where the Cars Come
  From · What the Carrier Rate Does Not Include.
- **Framing/compliance:** every dollar labelled a **carrier rate** (matching the calculator); Y7 named a
  Licensed & Bonded FMCSA broker (MC #1741537); warehouses referred to only as "export warehouses in the
  Newark / Irvington area" — no company name, no address; no "insured", no GPS/real-time, no "we transport".
- CTA into the existing funnel only: homepage calculator + `/quote` (no new form).

### Every published figure traced to §2

| Figure on the page | §2 fact-table source |
|---|---|
| 586 dispatches, 27 Aug 2025 – 23 Jul 2026, 213 carriers | Sample |
| Distance explains 88% | Distance explains |
| $2.56 / $1.42 / $1.05 / $0.79 / $0.61 / $0.52 per mile at 71/190/356/676/930/1,294 mi | $/mile decay |
| Auction brands within ±3% | Auction brand effect |
| ~$70 flat non-auction pickup; +48% <100 mi, +14% >500 mi | Non-auction pickup (+ percentage row) |
| SUV +5%, minivan +8%, pickup +17% vs sedan | Vehicle size vs sedan |
| No measurable difference between NJ warehouses | Delivery point inside NJ |
| 2026 ~7% above Aug–Dec 2025 | Market level |
| No winter premium; December 2025 among the cheapest | Seasonality |
| 25 states, 79 pickup locations (3+ each) | Origin coverage |
| Top states NY, PA, IL, MA, NJ, OH, IN, VA, MD, MN, MI, CT (volume order, no $) | Top origin states |
| Y7 fee: dealers/exporters $50 ($60 when Y7 pays), individuals $75 or 10% | `[NJPORTS-T1]` (permitted) |

Grep of the prerendered page confirms **only** these dollar values appear: `$0.52 $0.61 $0.79 $1.05
$1.42 $2.56 $50 $60 $70 $75` — all from §2. **No `TODO(sergii)` markers were needed** — every figure the
copy required is in the table. (One self-caught draft error — an invented "$2.00/$0.60 per mile"
illustration — was corrected to the table values $2.56 / $0.52 before build.)

## T2 — `/auction-to-port-transport`

Appended one new H2 section, **"New Jersey Export-Warehouse Pricing, Explained"**, after the "Popular
Auction-to-Port Corridors" section: 3 sentences summarising the flow + a Body-Link to the new page. No
existing heading reordered or rewritten; no pricing methodology duplicated. Verified in the browser: the
section renders and its link (`how NJ export-warehouse shipping cost is formed`) resolves to the new page (200).

## T3 — `/new-jersey-auto-transport`

Appended one new H2 section, **"The NJ Export-Warehouse Market"** (added a `Link` import). Owns
**NJ-as-a-market** (national feeder: 25 states / 79 locations, top states in volume order; warehouse
cluster = one delivery point) and links to the new page, which **owns the pricing methodology** —
per the cannibalisation guard. Distinct from the page's existing Port-Newark-terminal content.

## T4 — Crosslinks

- **`/services`:** added a card under the existing **"Popular Routes"** H2 (`routePages` +
  `cards.routes` index 5). **Decision: translated into all four locales (en/ru/pl/ua), not EN-gated.**
  *Why:* the route/location cards are position-indexed i18n arrays consumed in every locale
  (`cardFor(cardsRoutes, i)`); a missing index would break ru/pl/ua, and every existing EN-only lane page
  is already linked here from all four locales the same way. Translating is both the structurally-required
  and consistent choice. The card carries **no dollar figures**. Locale key counts stay equal (105 each,
  6 route cards each).
- **`/exporters`: SKIPPED (reported per instruction).** `/exporters` is a frozen-contract page
  (`Exporters.jsx:24-36` freezes heading text/levels, i18n keys, schema call sites, EntityTldr position,
  the 5 crosslinks, and dark/light band alternation). A Body-Link would require editing a frozen i18n
  string or inserting structural JSX into a translated paragraph across 4 locales — it cannot be done
  "inside an existing paragraph without touching a heading, schema call site, or band alternation." The
  new page still links **to** `/exporters` (related pill + primaryCTA), so the connection exists.

## T5 — Compliance fix (carry-over)

`src/pages/seo/CarShippingCost.jsx` (separate commit):
- **`:92`** — "…quotes that cover carrier transport, insurance, and door-to-door service" → "As a
  licensed FMCSA broker, Y7 Logistics provides an all-inclusive quote covering the carrier transport,
  **the carrier's cargo insurance**, and door-to-door coordination — Y7 is the broker and does not insure
  vehicles itself."
- **`:257`** — "…that covers the carrier's rate, insurance, and our coordination…" → "…the carrier's
  rate, **the carrier's cargo insurance**, and our **broker** coordination…"

---

## Verification

1. **`npm run build`** — clean. Prerender **139 OK, 0 failed** (was 138 → route count rose by one). New
   page `dist/nj-export-warehouse-shipping-cost/index.html` present.
2. **`npm run seo:check`** — 7 changes, **all intended and additive** (full diff below). No change to any
   title, description, canonical, hreflang, or JSON-LD on any page; no unintended page appears.
3. **`npm run preview`** (:4180) — new page + both extended pages return 200 and render; the
   auction-to-port Body-Link resolves to the new page. New page DOM: H1 correct, 9 content H2 sections,
   2 tables, EntityTldr, 6 FAQ items; document title = the page title.
4. **JSON-LD** on the new page: 2 blocks parse — `LocalBusiness` (root) and `BreadcrumbList + FAQPage +
   Service`. The 6 FAQ questions render from the same array that feeds the schema, so schema and visible
   copy match by construction.
5. **Locale parity** — `services.json` en/ru/pl/ua = 105 keys each, 6 route cards each.
6. **Forbidden-term grep** on new + edited copy: `insured / GPS / real-time / we transport / our
   trucks / our fleet / our drivers` → the only hit is `NewJerseyAutoTransport.jsx:70` ("Is my vehicle
   insured…"), which is **pre-existing FAQ content I did not touch** (my T3 addition contains no such
   term). Prerendered new page: **zero forbidden terms**.

### `seo:check` diff (in full)

```
~ /services :: headings        + h3 "NJ Export-Warehouse Pricing"  (under Popular Routes)
~ /services :: internalLinks   {total 81→82, unique 51→52}
~ /new-jersey-auto-transport :: headings       + h2 "The NJ Export-Warehouse Market"
~ /new-jersey-auto-transport :: internalLinks  {total 61→62, unique 46→47}
~ /auction-to-port-transport :: headings       + h2 "New Jersey Export-Warehouse Pricing, Explained"
~ /auction-to-port-transport :: internalLinks  {total 71→72, unique 51→52}
+ route added: /nj-export-warehouse-shipping-cost
```

Every diff is a section or link I added, or the new route. Unchanged (correctly absent from the diff):
`/`, `/exporters`, `/ports/newark`, `/door-to-port-auto-transport`, `/texas-to-newark-port-auto-transport`,
`/chicago-to-port-newark-car-shipping`, `/new-jersey-to-florida-car-shipping`. Baseline refreshed to the
new 11-route state after capture.

**Build artifacts not committed:** `public/sitemap.xml` and `scripts/sitemap-lastmod.json` are
regenerated by the build (lastmod churn). The new page's sitemap entry comes from
`scripts/generateSitemap.js` (updated, committed); Railway's `prebuild` regenerates the deployed sitemap
with the new URL at deploy time. Left uncommitted, consistent with NJPORTS-1.

---

## Commits (HELD — not pushed)

- `[NJPORTS-T1]` — new page + registration + snapshot tracking: `NjExportWarehouseShippingCost.jsx`,
  `App.jsx`, `generateSitemap.js`, `prerender.mjs`, `seo-snapshot.mjs`, `seo-baseline.json`
- `[NJPORTS-T2]` — `AuctionToPort.jsx`
- `[NJPORTS-T3]` — `NewJerseyAutoTransport.jsx`
- `[NJPORTS-T4]` — `Services.jsx`, `locales/{en,ru,pl,ua}/services.json`
- `[NJPORTS-T5]` — `CarShippingCost.jsx`
- `[NJPORTS-2] docs` — this report

**STOP** — no push. `push ≠ live`: Railway prerender takes ~14 minutes after Sergii pushes.
